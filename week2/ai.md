# AI Usage Report

I used an AI assistant (Gemini) to help with the following:
* **Debugging Environment:** Troubleshooting `pip` vs `apt` installation issues on WSL.
* **Test Generation:** Creating the `test.py` script that generates temporary `.meme` files for testing.
* **Error Analysis:** Identifying the cause of `ValueError: cannot convert float NaN to integer` (missing pseudocounts).
